package tests;

public class CustomerTest {
    /*
    private final Logger log = Logger.getLogger(EpUiTests.class);

   @Test(groups = {"smoke"}, priority = 2)
   public void searchByOrderId() {
       LoginPage loginPage = new LoginPage();
       log.info("Launching the browser");
       driver.get(super.url);
       TestUtils.wait(5);
       log.info("Login ...");
       HomePage homePage = loginPage.login(super.username, super.password);
       log.info("####### Page title : " + driver.getTitle());
       TestUtils.wait(5);
       CustomersPage customersPage = homePage.goToCustomers();
       TestUtils.wait(5);
       log.info("####### Page title : " + driver.getTitle());
       customersPage.customersTab.click();
       TestUtils.wait(5);
       customersPage.ordersTab.click();
       TestUtils.wait(5);
       customersPage.orderNumber.sendKeys("87089" + Keys.ENTER);
//        customersPage.orderNumber.clear();
//        customersPage.orderNumber.sendKeys("12345");
//        customersPage.calendar.click();
//        customersPage.logout();
       TestUtils.wait(5);
   }

     */
}
